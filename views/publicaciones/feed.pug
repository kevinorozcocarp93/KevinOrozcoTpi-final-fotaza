doctype html
html
  head
    title Feed General - Fotaza2

    link(
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    )

  body.bg-light

    .container.mt-4

      .d-flex.justify-content-between.align-items-center.mb-4

        h1 Feed General

        div

          a.btn.btn-primary.me-2(
            href="/perfil"
          )
            | Mi Perfil

          a.btn.btn-danger(
            href="/auth/logout"
          )
            | Cerrar Sesión

      each post in posts

        .card.mb-4.shadow-sm

          .card-body

            h3.card-title= post.titulo

            p.text-muted
              strong Autor:
              |  #{post.User.nombre}

            p.card-text= post.descripcion

            if post.imagen

              img.img-fluid.rounded.mb-3(
                src=`/uploads/${post.imagen}`
              )

            p.fw-bold.text-primary
              | 👍 #{post.Likes.length} Me gusta

            h5.mt-4 Comentarios

            if post.Comments.length > 0

              each comentario in post.Comments

                .border.rounded.p-2.mb-2.bg-light

                  strong= comentario.User.nombre + ': '

                  | #{comentario.contenido}

            else

              p.text-muted No hay comentarios todavía.